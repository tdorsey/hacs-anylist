# Environment Configuration Template for HACS Anylist TypeScript Project
# Copy this file to .env for local development
# Never commit actual .env files with sensitive data

# === Application Configuration ===
NODE_ENV=development
LOG_LEVEL=debug
PORT=3000

# === Anylist Configuration ===
ANYLIST_EMAIL=your-email@example.com
ANYLIST_PASSWORD=your-anylist-password
ANYLIST_SERVER_PORT=3000
ANYLIST_DEFAULT_LIST=Shopping

# === Database Configuration ===
POSTGRES_HOST=postgres-dev
POSTGRES_PORT=5432
POSTGRES_DB=anylist_dev
POSTGRES_USER=anylist
POSTGRES_PASSWORD=development_password

# === Redis Configuration ===
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=anylist123

# === Security Configuration ===
JWT_SECRET=your-jwt-secret-key-here
API_KEY=your-api-key-here
ENCRYPTION_KEY=your-encryption-key-here

# === Home Assistant Configuration ===
HASS_URL=http://homeassistant:8123
HASS_TOKEN=your-home-assistant-long-lived-access-token

# === Docker Configuration ===
COMPOSE_PROJECT_NAME=hacs-anylist
DOCKER_BUILDKIT=1

# === Development Configuration ===
DEBUG=anylist:*
WATCH_FILES=true
HOT_RELOAD=true

# === Production Configuration ===
# Override these in production
# NODE_ENV=production
# LOG_LEVEL=info
# ENABLE_METRICS=true

# === SSL/TLS Configuration (Production) ===
# SSL_CERT_PATH=/etc/ssl/certs/cert.pem
# SSL_KEY_PATH=/etc/ssl/private/key.pem
# DOMAIN=your-domain.com

# === Monitoring Configuration ===
# PROMETHEUS_ENABLED=true
# PROMETHEUS_PORT=9090
# GRAFANA_ENABLED=true
# GRAFANA_PORT=3001

# === Timezone ===
TZ=UTC